apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
type: Opaque
# POC-only defaults live in values.yaml; override via --set or a values file for real credentials.
stringData:
  mysql_root_password: {{ .Values.mysql.secret.rootPassword | quote }}
  mysql_admin_password: {{ .Values.mysql.secret.adminPassword | quote }}
